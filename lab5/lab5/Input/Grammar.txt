program antet declarationstmt assignstmt stmtlist iostmt forstmt compstmt condstmt expression operator term stmt boolexpr increment_decrement booloperator relation condition addsub term prodterm value sum unit produnit
{ return const ; } include < > id using namespace int ( ) = + - * / % cin >> cout << for double , <= >= == != && || ++ -- if
program
program?antet { stmtlist return const }
antet?include < id > using namespace id ; int id ( )
stmtlist?stmt
stmt?declarationstmt
stmt?assignstmt
stmt?iostmt
stmt?forstmt
stmt?compstmt
stmt?condstmt
compstmt?{ stmtlist }
declarationstmt?int id ;
declarationstmt?double id ;
assignstmt?id = expression ;
expression?term operator const
term?const
operator?+
operator?-
operator?*
operator?/
operator?%
iostmt?cin >> id ;
iostmt?cout << id ;
condition?expression relation expression
relation?<
relation?<=
relation?>
relation?>=
relation?==
relation?!=
booloperator?&&
booloperator?||
condstmt?if ( condition ) { stmtlist }